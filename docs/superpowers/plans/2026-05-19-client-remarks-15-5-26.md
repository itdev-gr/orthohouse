# Client Remarks 15/5/2026 — Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Implement all 17 corrections from the client PDF dated 15/5/2026 (`Remarks 15-5-26.pdf`) on the OrthoHouse Cyprus Astro storefront — covering header logo, info box separation, trust-badge icons, footer contact alignment, quotation messaging for Sensory Rooms & Stairlifts, Orthopaedics page label, in-stock badges on product cards, "Similar Products" + "Recently Viewed", full breadcrumbs site-wide, image lightbox, and a clearer Add-to-Cart flow.

**Architecture:** This is a static Astro site (Astro 5, no React/Vue, Shopify Storefront API via a small `lib/shopify.ts`). All UI lives in `.astro` files. There is no test harness in the repo (no `vitest`, `playwright` config, etc. — only `astro dev`/`build`). "Tests" in this plan are therefore **manual verification steps**: build the site, run dev server, inspect each affected page, and screenshot-check the change. Each task ends with a verification step + a git commit. Translation keys live in `src/i18n/ui.ts`; reuse this file for all user-visible strings (EN + EL).

**Tech Stack:** Astro 5.3, TypeScript, @shopify/storefront-api-client, vanilla JS (no framework), CSS (no Tailwind), `src/i18n/ui.ts` for translations.

**Open client decisions made pragmatically** (flag to user, do not block on):
- PDF #13: "Where does the `Products` breadcrumb link go?" → home page anchor `#cat-section` (the "Shop by Category" grid). There is no `/products` listing page.
- PDF #17: "Where does `Continue Shopping` go?" → cart-empty link stays home; the new Add-to-Cart confirmation modal's "Continue Shopping" button closes the modal and stays on the current product page.

**Blocked on client deliverables** (placeholder tasks, do not implement final content yet):
- PDF #1: Replacement copy for the "Why Customers Trust Us" info box — client said *"Θα στείλω ξεχωριστή σημείωση"*. We will **visually separate** the box now and leave a `TODO(client-copy)` comment with the current text intact.
- PDF #2: Higher-resolution logo PNG — client said *"Θα σας το στείλω σε .png"*. We will swap in a local `/images/logo.png` reference pointing to a placeholder asset; the file itself is committed when the client provides it.

---

## File Structure (what each task touches)

| File | Role |
| --- | --- |
| `src/layouts/Layout.astro` | Header logo (L102-104), footer contact (L452-472), footer hours (L469) — task 2 & 4 |
| `src/pages/index.astro` | Info box "Why Customers Trust Us" (L97-102), trust bar (L416-481) — task 1 & 3 |
| `src/pages/collections/[handle].astro` | Featured products section (L132-165), product card price/availability (L132-165 & L177-205) — tasks 5, 7, 9 |
| `src/pages/products/[handle].astro` | Breadcrumbs (L62-145, L596 CSS), gallery + lightbox (L150-190), price block (L218-230), add-to-cart (L281-289, L1677-1705), "More from" (L508-558) — tasks 5, 10, 12, 15, 16 |
| `src/pages/cart.astro` | Continue Shopping link (L19), checkout flow already simple — task 17 |
| `src/lib/menuConfig.ts` | Orthopaedics children already exist (L88-98) — task 6 verification only |
| `src/lib/productData.ts` | Optional: per-product extended data (sensory/stairlift handles for hide-price logic) |
| `src/i18n/ui.ts` | All new translation keys (EN/EL) |
| `src/pages/pages/about-us.astro`, `find-us.astro`, `working-hours.astro`, `shipping-delivery.astro`, `legal-notice.astro`, `refund-cancellation-policy.astro`, `terms-and-conditions.astro`, `terms-of-service.astro`, `cookies-policy.astro` | Add breadcrumbs — task 14 |
| `src/pages/search.astro` | Add breadcrumbs — task 14 |
| `src/pages/cart.astro` | Add breadcrumbs — task 14 |
| `src/pages/index.astro` | (no breadcrumbs on home — convention) |
| **New** `src/components/Breadcrumb.astro` | Shared breadcrumb component (extracted from product page logic) |
| **New** `src/components/TrustBadgesStrip.astro` | 6-icon strip for PDF #3 |
| **New** `src/components/RecentlyViewed.astro` | Client-side LocalStorage-driven recently-viewed strip |
| **New** `public/cart-feedback.css` *(or inline)* | Styles for new Add-to-Cart confirmation modal |
| **New** `public/images/logo.png` | Client-supplied higher-res logo (placeholder until delivered) |

---

## Task 1 — PDF #1: Visually separate the "Why Customers Trust Us" info box from surrounding content

**PDF text:** *"Αυτό το ενημερωτικό πλαίσιο να ξεχωρίσει από το πλαίσιο του Orthopeadics και ταυτόχρονα να τροποποιηθεί. Θα στείλω ξεχωριστή σημείωση για το πως θα είναι και τι θα γράφει."*

**Translation:** Separate the info box visually from the Orthopaedics section + revise the copy (copy is **deferred** — client sending separate note).

**Files:**
- Modify: `src/pages/index.astro:97-102`

- [ ] **Step 1: Confirm the current markup.** Open `src/pages/index.astro:97-102`. You should see:

  ```astro
  <section class="trust-section">
    <h2 class="trust-section-title">{t("home.trust.title")}</h2>
    <p class="trust-section-text">
      {t("home.trust.body")}
    </p>
  </section>
  ```

- [ ] **Step 2: Add a `data-todo` marker for the client copy (do not touch text yet).** Edit `src/pages/index.astro:97-102` to:

  ```astro
  <!-- TODO(client-copy 2026-05-19): client to supply new copy/title; see Remarks 15-5-26.pdf §1 -->
  <section class="trust-section trust-section--card" aria-labelledby="trust-section-heading">
    <div class="trust-section-inner">
      <h2 id="trust-section-heading" class="trust-section-title">{t("home.trust.title")}</h2>
      <p class="trust-section-text">
        {t("home.trust.body")}
      </p>
    </div>
  </section>
  ```

- [ ] **Step 3: Find the existing `.trust-section` CSS in `src/pages/index.astro` and add a `--card` modifier.** Use Grep on the file for `.trust-section ` and append (do not replace) under the matched ruleset:

  ```css
  /* Card-style separation per PDF §1 */
  .trust-section--card {
    margin: 3rem auto;
    max-width: min(1100px, calc(100% - 3rem));
    background: #ffffff;
    border: 1px solid #e8edf3;
    border-radius: 16px;
    box-shadow: 0 8px 24px rgba(15, 23, 42, 0.06);
    padding: 2.5rem 2rem;
    text-align: center;
  }
  .trust-section--card .trust-section-inner {
    max-width: 820px;
    margin-inline: auto;
  }
  @media (max-width: 640px) {
    .trust-section--card { padding: 1.75rem 1.25rem; }
  }
  ```

- [ ] **Step 4: Verify by running the dev server.**

  Run: `npm run dev`
  Open: `http://localhost:4321/`
  Expected: The "Why Customers Trust Us" block is now visually a centred white card with rounded corners and a soft shadow, clearly separated from the hero above and the Orthopaedic showcase below.

- [ ] **Step 5: Commit.**

  ```bash
  git add src/pages/index.astro
  git commit -m "feat(home): visually separate trust info box per remarks §1

  Adds a card-style frame (.trust-section--card) around the
  'Why Customers Trust Us' block so it no longer blends into the
  surrounding sections. Copy revision deferred — client to supply text."
  ```

---

## Task 2 — PDF #2: Swap header logo for higher-resolution local PNG

**PDF text:** *"Το λογότυπο της Orthohouse πάνω αριστερά να είναι καλύτερης ανάλυσης όπως αυτό της υφιστάμενης (παλιάς) ιστοσελίδας. Θα σας το στείλω σε .png."*

**Translation:** Logo top-left should be higher resolution, like the existing site. Client will send .png.

**Files:**
- Modify: `src/layouts/Layout.astro:102-104`
- Create (placeholder): `public/images/logo.png` (commit a small README pointing to the missing asset until client delivers)

- [ ] **Step 1: Add the local logo asset path placeholder.** Edit `src/layouts/Layout.astro:102-104`:

  Replace:
  ```astro
  <a href={lp("/")} class="nav-logo">
    <img src="https://orthohouse.com.cy/cdn/shop/files/logo_website.png?v=1767804500&width=240" alt="OrthoHouse Cyprus" class="nav-logo-img" width="240" height="60" />
  </a>
  ```

  With:
  ```astro
  <a href={lp("/")} class="nav-logo" aria-label="OrthoHouse Cyprus — home">
    <!-- TODO(client-asset 2026-05-19): replace /images/logo.png with high-res file from client (Remarks 15-5-26.pdf §2) -->
    <img
      src="/images/logo.png"
      alt="OrthoHouse Cyprus"
      class="nav-logo-img"
      width="480"
      height="120"
      srcset="/images/logo.png 1x, /images/logo@2x.png 2x"
      fetchpriority="high"
      decoding="async"
    />
  </a>
  ```

- [ ] **Step 2: Create placeholder asset directory.**

  Run:
  ```bash
  mkdir -p public/images
  ```

  If `public/images/logo.png` does not yet exist, copy the current remote logo as a temporary placeholder so the site does not 404 in dev:

  ```bash
  curl -L "https://orthohouse.com.cy/cdn/shop/files/logo_website.png?v=1767804500&width=480" -o public/images/logo.png
  curl -L "https://orthohouse.com.cy/cdn/shop/files/logo_website.png?v=1767804500&width=960" -o public/images/logo@2x.png
  ```

  *(These are temporary; replace with the client-supplied PNG when received.)*

- [ ] **Step 3: Verify in browser.** Run `npm run dev`, open `http://localhost:4321/`, hard-reload (`Cmd-Shift-R`).
  Expected: logo renders correctly top-left, no broken-image icon, no 404 in DevTools Network tab.

- [ ] **Step 4: Commit.**

  ```bash
  git add src/layouts/Layout.astro public/images/logo.png public/images/logo@2x.png
  git commit -m "feat(layout): serve logo locally with 2x asset per remarks §2

  Switches header logo from external Shopify CDN URL to a local
  asset so we can swap in the higher-resolution PNG the client is
  sending. Adds @2x source for retina displays."
  ```

---

## Task 3 — PDF #3: Replace 4-icon trust bar with 6-icon trust-badge strip

**PDF text:** *"Να μπουν εικονίδια για τις πιο κάτω περιγραφές꞉ Wolt Delivery / Authorised Distributor / EU Certified Products / Custom Fitting / Free Delivery / Customer Satisfaction"*

**Files:**
- Create: `src/components/TrustBadgesStrip.astro`
- Modify: `src/pages/index.astro:416-481` (replace existing `.trust-bar` block)
- Modify: `src/i18n/ui.ts` (add 12 new keys × 2 langs = 24 strings)

- [ ] **Step 1: Add translation keys.** Open `src/i18n/ui.ts`. After the existing `trustbar.freeShipping.sub` line (L78) in the **`en`** block, insert (before the blank line that separates the next group):

  ```typescript
      "trustbar.wolt.title": "Wolt Delivery",
      "trustbar.wolt.sub": "For all products marked with the Wolt logo",
      "trustbar.authorised.title": "Authorised Distributor",
      "trustbar.authorised.sub": "Trusted partner for genuine products",
      "trustbar.euCertified.title": "EU Certified Products",
      "trustbar.euCertified.sub": "Quality approved across Europe",
      "trustbar.customFitting.title": "Custom Fitting",
      "trustbar.customFitting.sub": "Tailored solutions with expert advice",
      "trustbar.freeDelivery.title": "Free Delivery",
      "trustbar.freeDelivery.sub": "Nationwide on orders over €100",
      "trustbar.satisfaction.title": "Customer Satisfaction",
      "trustbar.satisfaction.sub": "Committed to exceeding expectations",
  ```

  Mirror in the **`el`** block (after `trustbar.freeShipping.sub` on L194):

  ```typescript
      "trustbar.wolt.title": "Παράδοση μέσω Wolt",
      "trustbar.wolt.sub": "Για όλα τα προϊόντα με το σήμα Wolt",
      "trustbar.authorised.title": "Επίσημος Διανομέας",
      "trustbar.authorised.sub": "Αξιόπιστος συνεργάτης για γνήσια προϊόντα",
      "trustbar.euCertified.title": "Προϊόντα Πιστοποιημένα από ΕΕ",
      "trustbar.euCertified.sub": "Ποιότητα εγκεκριμένη σε όλη την Ευρώπη",
      "trustbar.customFitting.title": "Εξατομικευμένη Εφαρμογή",
      "trustbar.customFitting.sub": "Λύσεις στα μέτρα σας με εξειδικευμένη συμβουλή",
      "trustbar.freeDelivery.title": "Δωρεάν Παράδοση",
      "trustbar.freeDelivery.sub": "Πανελλήνια σε παραγγελίες άνω των €100",
      "trustbar.satisfaction.title": "Ικανοποίηση Πελατών",
      "trustbar.satisfaction.sub": "Δεσμευμένοι να υπερβαίνουμε τις προσδοκίες",
  ```

- [ ] **Step 2: Create the component.** Create `src/components/TrustBadgesStrip.astro`:

  ```astro
  ---
  import { pickLang, useTranslations } from "../i18n/utils";

  const lang = pickLang(Astro.currentLocale, Astro.url);
  const t = useTranslations(lang);

  const badges = [
    { key: "wolt",          icon: "M7 7h10v2H7zm0 4h10v2H7zm0 4h7v2H7zM3 5h2v14H3zm16 0h2v14h-2z" },
    { key: "authorised",    icon: "M12 2 4 5v6c0 5 3.4 9.4 8 11 4.6-1.6 8-6 8-11V5l-8-3zm-1 14-4-4 1.4-1.4L11 13.2l5.6-5.6L18 9l-7 7z" },
    { key: "euCertified",   icon: "M12 2C6.5 2 2 6.5 2 12s4.5 10 10 10 10-4.5 10-10S17.5 2 12 2zm0 18a8 8 0 1 1 0-16 8 8 0 0 1 0 16zm-1-13h2v2h-2V7zm0 4h2v6h-2v-6z" },
    { key: "customFitting", icon: "M17.7 6.3a1 1 0 0 0-1.4 0L4 18.6V20h1.4L17.7 7.7a1 1 0 0 0 0-1.4zM20.7 4.3l-1-1a1 1 0 0 0-1.4 0L17 4.6 19.4 7l1.3-1.3a1 1 0 0 0 0-1.4z" },
    { key: "freeDelivery",  icon: "M3 7h11v8H3V7zm12 2h3l3 3v3h-6V9zM5.5 18a1.5 1.5 0 1 0 0-3 1.5 1.5 0 0 0 0 3zm12 0a1.5 1.5 0 1 0 0-3 1.5 1.5 0 0 0 0 3z" },
    { key: "satisfaction",  icon: "M12 21s-7-4.5-7-10a4.5 4.5 0 0 1 8-2.7A4.5 4.5 0 0 1 19 11c0 5.5-7 10-7 10z" },
  ] as const;
  ---

  <section class="badges-strip" aria-label={lang === "el" ? "Λόγοι εμπιστοσύνης" : "Why shop with us"}>
    <ul class="badges-grid">
      {badges.map((b) => (
        <li class="badge-item">
          <svg class="badge-icon" xmlns="http://www.w3.org/2000/svg" viewBox="0 0 24 24" width="36" height="36" aria-hidden="true">
            <path d={b.icon} fill="currentColor"/>
          </svg>
          <span class="badge-title">{t(`trustbar.${b.key}.title` as any)}</span>
          <span class="badge-sub">{t(`trustbar.${b.key}.sub` as any)}</span>
        </li>
      ))}
    </ul>
  </section>

  <style>
    .badges-strip {
      background: #f8fafc;
      padding: 2.5rem 1.5rem;
      border-top: 1px solid #e8edf3;
      border-bottom: 1px solid #e8edf3;
    }
    .badges-grid {
      list-style: none;
      margin: 0 auto;
      padding: 0;
      max-width: 1200px;
      display: grid;
      grid-template-columns: repeat(6, minmax(0, 1fr));
      gap: 1.5rem;
    }
    .badge-item {
      display: flex;
      flex-direction: column;
      align-items: center;
      text-align: center;
      gap: 0.4rem;
      color: #1e3a5f;
    }
    .badge-icon { color: #2563eb; }
    .badge-title { font-weight: 700; font-size: 0.92rem; }
    .badge-sub { font-size: 0.78rem; color: #555; max-width: 18ch; line-height: 1.35; }

    @media (max-width: 960px) { .badges-grid { grid-template-columns: repeat(3, 1fr); } }
    @media (max-width: 520px) { .badges-grid { grid-template-columns: repeat(2, 1fr); } }
  </style>
  ```

- [ ] **Step 3: Mount the component on the home page and remove the legacy 4-icon trust bar.** Edit `src/pages/index.astro`:

  At the top of the frontmatter (top of the file, near other imports), add:
  ```astro
  import TrustBadgesStrip from "../components/TrustBadgesStrip.astro";
  ```

  Then replace the entire block at lines **416-481** (`<!-- TRUST / FEATURES BANNER -->` through `</div>` closing `.trust-bar`) with:

  ```astro
  <!-- ══════════════════════════════════════
       TRUST / FEATURES BANNER (6 badges per PDF §3)
       ══════════════════════════════════════ -->
  <TrustBadgesStrip />
  ```

- [ ] **Step 4: Delete the now-orphan `.trust-bar` CSS.** In `src/pages/index.astro` (search the `<style>` block at the bottom), delete every selector starting with `.trust-bar`, `.trust-item`, `.trust-icon`, `.trust-text`, `.trust-label`, `.trust-sub`, `.trust-divider`. Leave `.trust-section` and `.trust-section--card` (used in Task 1).

- [ ] **Step 5: Verify in browser.** `npm run dev` → `http://localhost:4321/`. Scroll to where the 4-icon trust bar used to be: should now show six tiles (Wolt, Authorised, EU Certified, Custom Fitting, Free Delivery, Customer Satisfaction) each with an icon, bold title, and 1-line subtitle. Resize the viewport to 640px wide → tiles re-flow to 3 then 2 columns. Toggle language via header switcher → Greek copy appears.

- [ ] **Step 6: Commit.**

  ```bash
  git add src/components/TrustBadgesStrip.astro src/pages/index.astro src/i18n/ui.ts
  git commit -m "feat(home): six trust badges strip per remarks §3

  Replaces the 4-icon trust bar with a 6-tile strip showing
  Wolt Delivery, Authorised Distributor, EU Certified Products,
  Custom Fitting, Free Delivery, and Customer Satisfaction.
  Adds EN/EL translation keys for each."
  ```

---

## Task 4 — PDF #4: Align footer Contact section with Working Hours page wording

**PDF text:** *"Στο Footer - Contact꞉ Όπως αυτά αναγράφονται στο Working hours."*

**Translation:** Footer contact info should be written exactly as it is on the Working Hours page.

**Files:**
- Read first: `src/pages/pages/working-hours.astro` — capture the canonical address, phone, email, and hours strings.
- Modify: `src/i18n/ui.ts` (update `footer.address`, `footer.hours`, plus add phone/email keys if they differ)
- Modify: `src/layouts/Layout.astro:452-472` (footer Contact column)

- [ ] **Step 1: Read the canonical wording.**

  Run:
  ```bash
  grep -nE "(Limassol|Cyprus|@orthohouse|\+357|Mon|Δευ|9:00|Friday|Λεμεσός)" src/pages/pages/working-hours.astro
  ```

  Copy the **exact** strings used on that page (address line, phone, email, hours). If the file uses translation keys, jump to those keys in `src/i18n/ui.ts` and capture both EN + EL.

- [ ] **Step 2: Reconcile the footer translation keys.** In `src/i18n/ui.ts`:
  - If working-hours.astro uses different address/phone/email/hours strings than `footer.address` / `footer.hours`, update the footer keys to match exactly.
  - Add two new keys if they don't already exist: `footer.phone` and `footer.email`, populated with the values from working-hours.astro.

  Example (replace the two existing footer.address/hours pairs and add the new ones):

  ```typescript
  "footer.address": "<exact address line(s) from working-hours.astro>",
  "footer.phone":   "<exact phone from working-hours.astro>",
  "footer.email":   "<exact email from working-hours.astro>",
  "footer.hours":   "<exact hours string from working-hours.astro>",
  ```

  Mirror in `el`.

- [ ] **Step 3: Update Layout.astro footer Contact column.** Edit `src/layouts/Layout.astro:452-472`:

  Replace the hard-coded phone (L461) and hard-coded email (L465) with translation keys:

  ```astro
  <li>
    <svg ...><!-- existing phone icon --></svg>
    <a href={`tel:${t("footer.phone").replace(/[^\d+]/g, "")}`}>{t("footer.phone")}</a>
  </li>
  <li>
    <svg ...><!-- existing mail icon --></svg>
    <a href={`mailto:${t("footer.email")}`}>{t("footer.email")}</a>
  </li>
  ```

  Leave the address `<a href={lp("/pages/find-us")}>{t("footer.address")}</a>` and the hours `{t("footer.hours")}` lines as-is (only the *values* change, in `ui.ts`).

- [ ] **Step 4: Verify side-by-side.** `npm run dev` → open `http://localhost:4321/pages/working-hours` in one tab and `http://localhost:4321/` (scroll to footer) in another. Confirm the address, phone, email, and hours strings are **identical**, character-for-character. Switch to `/el/` and re-check Greek.

- [ ] **Step 5: Commit.**

  ```bash
  git add src/i18n/ui.ts src/layouts/Layout.astro
  git commit -m "fix(footer): match Contact details to Working Hours page per remarks §4

  Footer Contact column now reuses the same address/phone/email/hours
  strings shown on /pages/working-hours so the two never drift.
  Adds footer.phone / footer.email translation keys (EN+EL)."
  ```

---

## Task 5 — PDF #5: Hide price for Sensory Rooms & Stairlifts, show consultation message

**PDF text:** *"Στην περίπτωση των Sensory Rooms και των Stairlifts δεν μπορεί να υπάρχει τιμή. Πρέπει να φαίνεται ένα μήνυμα π.χ. Contact us for free consultation and quotation."*

**Files:**
- Modify: `src/i18n/ui.ts` (add `product.contactForQuote` EN+EL)
- Create: `src/lib/quotationCollections.ts` (single source of truth — list of collection handles that hide price)
- Modify: `src/pages/products/[handle].astro:50-58, 218-230, 281-289`
- Modify: `src/pages/collections/[handle].astro:132-205` (product cards in both top-level and subcategory branches)

- [ ] **Step 1: Create the canonical collection list.** Create `src/lib/quotationCollections.ts`:

  ```typescript
  /**
   * Collections whose products are NOT sold with a public price.
   * For these, the storefront hides the price and shows the
   * "Contact us for free consultation and quotation" CTA instead
   * (per client remarks 15/5/2026 §5).
   *
   * Includes both the top-level category handles AND every child
   * subcategory handle from menuConfig.ts.
   */
  export const QUOTATION_COLLECTION_HANDLES = new Set<string>([
    // Sensory Rooms (top + children)
    "sensory-rooms",
    "sensory-rooms-products",
    "sensory-rooms-solutions",
    // Stair Lifts (single subcategory under Lifting Solutions)
    "stair-lifts-lehner",
  ]);

  /** True when *any* collection on the product is a quote-only one. */
  export function isQuotationOnly(
    collectionHandles: ReadonlyArray<string | null | undefined>,
  ): boolean {
    return collectionHandles.some(
      (h) => !!h && QUOTATION_COLLECTION_HANDLES.has(h),
    );
  }
  ```

- [ ] **Step 2: Add translation keys.** In `src/i18n/ui.ts`, after `"product.outOfStock"` in both blocks, add:

  ```typescript
  // English block:
  "product.contactForQuote": "Contact us for free consultation and quotation",
  "product.requestQuote": "Request a Quotation",

  // Greek block:
  "product.contactForQuote": "Επικοινωνήστε μαζί μας για δωρεάν συμβουλευτική και προσφορά",
  "product.requestQuote": "Ζητήστε Προσφορά",
  ```

- [ ] **Step 3: Update the product detail page.** Edit `src/pages/products/[handle].astro`:

  Near the top of the frontmatter, after the existing imports (around L1-11), add:
  ```typescript
  import { isQuotationOnly } from "../../lib/quotationCollections";
  ```

  After the existing `// Related products` block (around L49-57) — where `collectionHandle` is computed — add:
  ```typescript
  const allCollectionHandles: string[] = ((product as any).collections?.nodes ?? []).map((n: any) => n.handle);
  const quoteOnly = isQuotationOnly(allCollectionHandles);
  ```

  Then replace the price block at **L218-230** with:
  ```astro
  <!-- Price (or quote CTA for sensory-rooms / stair-lifts) -->
  <div class="price-block">
    {quoteOnly ? (
      <p class="price-quote">{t("product.contactForQuote")}</p>
    ) : isSale ? (
      <>
        <span class="price-compare">{currencyCode} {compareAt.toFixed(2)}</span>
        <span class="price-sale" id="variant-price">{currencyCode} {price.toFixed(2)}</span>
      </>
    ) : (
      <span class="price-regular" id="variant-price">
        {currencyCode} {price.toFixed(2)}
      </span>
    )}
  </div>
  ```

  And replace the Add-to-Cart button at **L281-289** with:
  ```astro
  {quoteOnly ? (
    <a
      class="add-to-cart-btn quote-btn"
      href={lp("/pages/find-us")}
    >
      {t("product.requestQuote")}
    </a>
  ) : (
    <button
      type="button"
      class="add-to-cart-btn"
      id="add-to-cart"
      data-variant-id={firstVariant?.id}
      disabled={!isAvailable}
    >
      {isAvailable ? (lang === "el" ? "Προσθήκη στο Καλάθι" : "Add to Cart") : (lang === "el" ? "Εξαντλημένο" : "Sold Out")}
    </button>
  )}
  ```

  Find the `.price-block` CSS in the same file's `<style>` and append:
  ```css
  .price-quote {
    font-size: 1.05rem;
    font-weight: 600;
    color: #1e3a5f;
    background: #f1f5fb;
    border-left: 3px solid #2563eb;
    padding: 0.75rem 1rem;
    margin: 0;
    border-radius: 6px;
  }
  .quote-btn { text-align: center; text-decoration: none; }
  ```

- [ ] **Step 4: Update the collections page (both top-level and subcategory branches).** Edit `src/pages/collections/[handle].astro`:

  At the top of the frontmatter (after existing imports), add:
  ```typescript
  import { QUOTATION_COLLECTION_HANDLES } from "../../lib/quotationCollections";
  const isQuoteCollection = QUOTATION_COLLECTION_HANDLES.has(handle);
  ```

  In **both** product-card grids — the "Featured products" grid (L132-165) **and** the subcategory grid (L177-205) — replace the `<p class="price">…</p>` block:

  From:
  ```astro
  <p class="price">
    {product.priceRange.minVariantPrice.currencyCode}{" "}
    {parseFloat(product.priceRange.minVariantPrice.amount).toFixed(2)}
  </p>
  ```

  To:
  ```astro
  {isQuoteCollection ? (
    <p class="price price-quote-card">{t("product.contactForQuote")}</p>
  ) : (
    <p class="price">
      {product.priceRange.minVariantPrice.currencyCode}{" "}
      {parseFloat(product.priceRange.minVariantPrice.amount).toFixed(2)}
    </p>
  )}
  ```

  In the file's `<style>` block, append:
  ```css
  .price-quote-card {
    font-size: 0.82rem;
    font-weight: 600;
    color: #1e3a5f;
    line-height: 1.3;
  }
  ```

- [ ] **Step 5: Verify.** `npm run dev`. Visit:
  - `http://localhost:4321/collections/sensory-rooms` → product cards (if any) show "Contact us for free consultation and quotation" instead of a price.
  - `http://localhost:4321/collections/stair-lifts-lehner` → same.
  - Any product detail page in those collections → price block replaced by the message, button reads "Request a Quotation" and links to `/pages/find-us`.
  - A normal collection like `/collections/orthopedic-braces-supports` → still shows numeric prices.

- [ ] **Step 6: Commit.**

  ```bash
  git add src/lib/quotationCollections.ts src/i18n/ui.ts src/pages/products/[handle].astro src/pages/collections/[handle].astro
  git commit -m "feat(products): hide price for sensory-rooms & stairlifts per remarks §5

  Adds lib/quotationCollections.ts as the single source of truth.
  Product detail and collection cards in those collections render
  'Contact us for free consultation and quotation' and a 'Request
  a Quotation' CTA linking to the Find Us page."
  ```

---

## Task 6 — PDF #6: Verify Orthopaedics subcategories appear in mega-menu

**PDF text:** *"Στο ανδρίκελο να μπουν οι υποκατηγορίες της κατηγορίας Orthopeadics."*

**Translation:** Put Orthopaedics subcategories in the mega-menu / dropdown.

**Status:** Already implemented in `src/lib/menuConfig.ts:88-98` (9 children: Neck, Back & Lumbar, Knee, Hip, Wrist & Thumb, Ankle, Elbow, Hand, Head). This task is **a verification + visual check only**.

**Files:**
- Verify only: `src/lib/menuConfig.ts:88-98`, `src/layouts/Layout.astro:162-212` (mega-panel render)

- [ ] **Step 1: Confirm subcategories exist.** Run:
  ```bash
  grep -A 12 '"Orthopaedics"' src/lib/menuConfig.ts | head -20
  ```
  Expected: prints the 9 children with their handles.

- [ ] **Step 2: Visual check.** `npm run dev` → open `http://localhost:4321/` → hover the "Orthopaedics" item in the dark blue category bar.
  Expected: A mega-panel slides down listing all 9 subcategories with their icons + descriptions.

- [ ] **Step 3: Mobile check.** Resize to 480px width → open the hamburger menu → expand "Orthopaedics" → expected: same 9 subcategories listed.

- [ ] **Step 4: If anything is missing, only then edit `menuConfig.ts`.** Otherwise, this task closes with no code change. Note "verified, no change" in the commit-style summary message to the user (no commit needed).

---

## Task 7 — PDF #7: Rename "Featured products" → "All Orthopaedic Products" on the Orthopaedics page only

**PDF text:** *"Στην επιλογή Orthopeadics... To featured products να γίνει ꞉ All Orthopeadic Products."*

**Files:**
- Modify: `src/i18n/ui.ts` (add `collection.allOrthopaedicProducts`)
- Modify: `src/pages/collections/[handle].astro:134`

- [ ] **Step 1: Add the new key.** In `src/i18n/ui.ts`, after `"collection.featuredProducts"` in both blocks, add:

  ```typescript
  // en:
  "collection.allOrthopaedicProducts": "All Orthopaedic Products",
  // el:
  "collection.allOrthopaedicProducts": "Όλα τα Ορθοπεδικά Προϊόντα",
  ```

- [ ] **Step 2: Make the heading collection-aware.** Edit `src/pages/collections/[handle].astro:134`:

  Replace:
  ```astro
  <h2 class="section-heading">{t("collection.featuredProducts")}</h2>
  ```

  With:
  ```astro
  <h2 class="section-heading">{
    handle === "orthopedic-braces-supports"
      ? t("collection.allOrthopaedicProducts")
      : t("collection.featuredProducts")
  }</h2>
  ```

- [ ] **Step 3: Verify.** `npm run dev` → visit `http://localhost:4321/collections/orthopedic-braces-supports`. The section under "Browse by category" should be titled **"All Orthopaedic Products"** (or "Όλα τα Ορθοπεδικά Προϊόντα" on `/el/`).

- [ ] **Step 4: Commit.**

  ```bash
  git add src/i18n/ui.ts src/pages/collections/[handle].astro
  git commit -m "feat(collections): rename featured-products to All Orthopaedic Products per remarks §7"
  ```

---

## Task 8 — PDF #8: Verify only Orthopaedics renders the bottom products grid

**PDF text:** *"Όλες οι άλλες κατηγορίες, η σελίδα τους σταματά στο Browse by Category."*

**Status:** Already correct — looking at `src/pages/collections/[handle].astro:107-165`, the `Browse by Category` + products grid block is wrapped in `{isTopLevel && ...}` with subcategories. We need to **change the rule**: only the **Orthopaedics** top-level page should render the products grid; other top-level pages stop at Browse by Category.

**Files:**
- Modify: `src/pages/collections/[handle].astro:132-165`

- [ ] **Step 1: Tighten the conditional.** Edit `src/pages/collections/[handle].astro:132`:

  Replace:
  ```astro
  {products.length > 0 && (
    <section class="products-section">
      <h2 class="section-heading">{
        handle === "orthopedic-braces-supports"
          ? t("collection.allOrthopaedicProducts")
          : t("collection.featuredProducts")
      }</h2>
  ```

  With:
  ```astro
  {handle === "orthopedic-braces-supports" && products.length > 0 && (
    <section class="products-section">
      <h2 class="section-heading">{t("collection.allOrthopaedicProducts")}</h2>
  ```

- [ ] **Step 2: Verify across categories.** `npm run dev`. For each top-level collection, the page should **stop at the subcategory grid** ("Browse by category"), with **no** "Featured products" section underneath:
  - `/collections/wheelchairs`
  - `/collections/walking-aids`
  - `/collections/health-medical-devices`
  - `/collections/kybun-shoes`
  - `/collections/home-care-daily-living`
  - `/collections/health-comfort`
  - `/collections/sensory-rooms`
  - `/collections/lifting-solutions`

  Only `/collections/orthopedic-braces-supports` should still show the products grid below Browse by Category, titled "All Orthopaedic Products".

- [ ] **Step 3: Commit.**

  ```bash
  git add src/pages/collections/[handle].astro
  git commit -m "fix(collections): only Orthopaedics renders the bottom products grid per remarks §8

  Top-level category pages other than orthopedic-braces-supports
  now stop at the Browse-by-Category subcategory grid."
  ```

---

## Task 9 — PDF #9: Show in-stock / out-of-stock badge on every product card

**PDF text:** *"Όταν εμφανίζονται τα προϊόντα της κάθε υποκατηγορίας, δεν θα ήταν πιο σωστό από εδώ να φαίνεται ποιά είναι in stock και ποιά out of stock?"*

**Files:**
- Modify: `src/lib/shopify.ts` — confirm `availableForSale` is already in `COLLECTION_PRODUCTS_QUERY` and `PRODUCT_BY_HANDLE_QUERY`; if not, add it.
- Modify: `src/i18n/ui.ts` (add `card.inStock`, `card.outOfStock`)
- Modify: `src/pages/collections/[handle].astro` — both card grids
- Modify: `src/pages/products/[handle].astro:508-558` (the related-products carousel cards)

- [ ] **Step 1: Confirm the Storefront query returns availability.**

  Run:
  ```bash
  grep -nB1 -A3 "availableForSale\|priceRange\|featuredImage" src/lib/shopify.ts | head -60
  ```

  Inspect the `COLLECTION_PRODUCTS_QUERY` constant. If `availableForSale` is **not** in the fragment, add it. The fragment should include something like:

  ```graphql
  nodes {
    id
    handle
    title
    availableForSale       # ← add if missing
    featuredImage { url altText }
    priceRange { minVariantPrice { amount currencyCode } }
  }
  ```

  If you edited the query, run `npm run build` once locally to confirm the Shopify call still resolves.

- [ ] **Step 2: Add translation keys.** In `src/i18n/ui.ts` (both blocks), append:

  ```typescript
  // en:
  "card.inStock": "In stock",
  "card.outOfStock": "Out of stock",
  // el:
  "card.inStock": "Διαθέσιμο",
  "card.outOfStock": "Εξαντλημένο",
  ```

- [ ] **Step 3: Render the badge on the top-level products grid.** Edit `src/pages/collections/[handle].astro` around L132-165, inside each `<div class="info">…</div>`, insert above the `.price` paragraph:

  ```astro
  <span class={"stock-pill " + (product.availableForSale ? "in" : "out")}>
    {product.availableForSale ? t("card.inStock") : t("card.outOfStock")}
  </span>
  ```

- [ ] **Step 4: Do the same on the subcategory grid (L177-205).** Same insertion inside `<div class="info">`.

- [ ] **Step 5: Add the badge to the related-products carousel.** Edit `src/pages/products/[handle].astro:540-545` — inside `<div class="rel-info">…</div>`, above the `<p class="rel-price">`, insert:

  ```astro
  <span class={"stock-pill " + (p.availableForSale ? "in" : "out")}>
    {p.availableForSale ? t("card.inStock") : t("card.outOfStock")}
  </span>
  ```

- [ ] **Step 6: Add shared pill styles.** Append to `src/pages/collections/[handle].astro` `<style>` and to `src/pages/products/[handle].astro` `<style>`:

  ```css
  .stock-pill {
    display: inline-flex;
    align-items: center;
    gap: 0.25rem;
    font-size: 0.7rem;
    font-weight: 600;
    padding: 0.18rem 0.45rem;
    border-radius: 999px;
    margin-bottom: 0.35rem;
  }
  .stock-pill.in  { background: #ecfdf5; color: #047857; }
  .stock-pill.out { background: #fef2f2; color: #b91c1c; }
  ```

- [ ] **Step 7: Verify.** Visit any subcategory page (e.g. `/collections/back-lumbar`). Each product card should display a small green "In stock" or red "Out of stock" pill above the price. Confirm at least one in-stock and one out-of-stock product render correctly.

- [ ] **Step 8: Commit.**

  ```bash
  git add src/lib/shopify.ts src/i18n/ui.ts src/pages/collections/[handle].astro src/pages/products/[handle].astro
  git commit -m "feat(cards): show in-stock / out-of-stock pill on product cards per remarks §9"
  ```

---

## Task 10 — PDF #10: Replace "More from {category}" with "Similar Products"

**PDF text:** *"Όπου γράφεται More from All να γίνει επι της ουσίας και να αναγράφεται ταυτόχρονα ꞉ Similar Products"*

**Files:**
- Modify: `src/i18n/ui.ts` (add `product.similarProducts`)
- Modify: `src/pages/products/[handle].astro:512`

- [ ] **Step 1: Add the key.** In `src/i18n/ui.ts`:

  ```typescript
  // en:
  "product.similarProducts": "Similar Products",
  // el:
  "product.similarProducts": "Παρόμοια Προϊόντα",
  ```

- [ ] **Step 2: Replace the heading.** Edit `src/pages/products/[handle].astro:512`:

  Replace:
  ```astro
  <h2 class="related-title">{lang === "el" ? "Περισσότερα από" : "More from"} {collectionTitle}</h2>
  ```

  With:
  ```astro
  <h2 class="related-title">{t("product.similarProducts")}</h2>
  ```

- [ ] **Step 3: Verify.** Any product page — scroll to the related carousel. Heading should read "Similar Products" (or "Παρόμοια Προϊόντα" in Greek). Confirm `collectionTitle` is no longer used here (it's still used elsewhere for breadcrumbs — do not remove the variable).

- [ ] **Step 4: Commit.**

  ```bash
  git add src/i18n/ui.ts src/pages/products/[handle].astro
  git commit -m "feat(product): rename related carousel heading to Similar Products per remarks §10"
  ```

---

## Task 11 — PDF #11: Add a "Recently Viewed Products" strip alongside Similar Products

**PDF text:** *"Στο τέλος της οποιασδήποτε σελίδας... εμφανίζονται τα προϊόντα Recently Viewed Products... Μπορούμε να αφήσουμε και τα δύο αν είναι εφικτό."*

**Translation:** Add a Recently Viewed Products section at the end of any page (home, category, product). Keep both Similar Products and Recently Viewed if possible.

**Files:**
- Create: `src/components/RecentlyViewed.astro`
- Modify: `src/pages/products/[handle].astro` — record current product to LocalStorage; mount component
- Modify: `src/pages/index.astro` — mount component near bottom
- Modify: `src/pages/collections/[handle].astro` — mount component near bottom
- Modify: `src/i18n/ui.ts` — add `product.recentlyViewed`

- [ ] **Step 1: Translation keys.** In `src/i18n/ui.ts` add:

  ```typescript
  // en:
  "product.recentlyViewed": "Recently Viewed",
  "product.recentlyViewedEmpty": "",  // empty string by design — strip hides if empty
  // el:
  "product.recentlyViewed": "Πρόσφατα Προβληθέντα",
  "product.recentlyViewedEmpty": "",
  ```

- [ ] **Step 2: Create the component.** Create `src/components/RecentlyViewed.astro`:

  ```astro
  ---
  import { pickLang, useTranslations, localizeHref } from "../i18n/utils";

  const lang = pickLang(Astro.currentLocale, Astro.url);
  const t = useTranslations(lang);
  const localePrefix = lang === "el" ? "/el" : "";
  ---

  <section class="recently-viewed" id="recently-viewed-section" hidden data-locale-prefix={localePrefix}>
    <h2 class="recently-viewed__title">{t("product.recentlyViewed")}</h2>
    <div class="recently-viewed__track" id="recently-viewed-track"></div>
  </section>

  <style>
    .recently-viewed {
      max-width: 1200px;
      margin: 3rem auto;
      padding: 0 1.5rem;
    }
    .recently-viewed__title {
      font-size: 1.25rem;
      font-weight: 700;
      color: #1e3a5f;
      margin: 0 0 1rem;
    }
    .recently-viewed__track {
      display: grid;
      grid-template-columns: repeat(auto-fill, minmax(180px, 1fr));
      gap: 1rem;
    }
    .rv-card {
      display: block;
      text-decoration: none;
      color: inherit;
      background: #fff;
      border: 1px solid #e8edf3;
      border-radius: 10px;
      overflow: hidden;
      transition: box-shadow .15s, transform .15s;
    }
    .rv-card:hover { box-shadow: 0 6px 16px rgba(15,23,42,.08); transform: translateY(-2px); }
    .rv-card img { width: 100%; height: 140px; object-fit: contain; background: #fff; }
    .rv-card__body { padding: 0.6rem 0.75rem 0.75rem; }
    .rv-card__title {
      font-size: 0.85rem;
      font-weight: 600;
      line-height: 1.3;
      margin: 0 0 0.3rem;
      display: -webkit-box;
      -webkit-line-clamp: 2;
      -webkit-box-orient: vertical;
      overflow: hidden;
    }
    .rv-card__price { font-size: 0.8rem; color: #2563eb; margin: 0; font-weight: 600; }
  </style>

  <script is:inline>
    (function () {
      var section = document.getElementById("recently-viewed-section");
      if (!section) return;
      var track = document.getElementById("recently-viewed-track");
      var prefix = section.dataset.localePrefix || "";

      var KEY = "ohc:recently-viewed";
      var items = [];
      try {
        items = JSON.parse(localStorage.getItem(KEY) || "[]");
      } catch (e) { items = []; }

      // Exclude the current product if we're on its page
      var currentHandle = (document.querySelector('[data-current-product-handle]') || {}).getAttribute &&
        document.querySelector('[data-current-product-handle]').getAttribute('data-current-product-handle');
      var filtered = items.filter(function (it) { return it && it.handle && it.handle !== currentHandle; });

      if (filtered.length === 0) return;

      track.innerHTML = filtered.slice(0, 8).map(function (it) {
        var price = it.price && it.currency ? (it.currency + " " + Number(it.price).toFixed(2)) : "";
        var img = it.image ? '<img src="' + it.image + '" alt="' + (it.title || "").replace(/"/g, "&quot;") + '" loading="lazy">' : "";
        return '<a class="rv-card" href="' + prefix + '/products/' + it.handle + '">' +
          img +
          '<div class="rv-card__body">' +
          '<p class="rv-card__title">' + (it.title || "") + '</p>' +
          (price ? '<p class="rv-card__price">' + price + '</p>' : "") +
          '</div></a>';
      }).join("");

      section.hidden = false;
    })();
  </script>
  ```

- [ ] **Step 3: Record the product when its page loads.** Edit `src/pages/products/[handle].astro`. Find the `<Layout ...>` opening tag (around L118) and add a `data-current-product-handle` attribute to the breadcrumb nav or to an early element. Easiest: edit L127 from:

  ```astro
  <nav class="breadcrumb" aria-label="Breadcrumb">
  ```
  to:
  ```astro
  <nav class="breadcrumb" aria-label="Breadcrumb" data-current-product-handle={product.handle}>
  ```

  Then add a small recording script. Find the end of the existing IIFE in the inline `<script>` block (just before `})();` on line **1726**) and insert immediately above that closing line:

  ```javascript
    /* ── Record this product in Recently Viewed ── */
    try {
      var KEY = "ohc:recently-viewed";
      var existing = [];
      try { existing = JSON.parse(localStorage.getItem(KEY) || "[]"); } catch (e) {}
      var entry = {
        handle: %%PRODUCT_HANDLE%%,
        title: %%PRODUCT_TITLE%%,
        image: %%PRODUCT_IMAGE%%,
        price: %%PRODUCT_PRICE%%,
        currency: %%PRODUCT_CURRENCY%%,
        viewedAt: Date.now()
      };
      var dedup = existing.filter(function (e) { return e.handle !== entry.handle; });
      dedup.unshift(entry);
      localStorage.setItem(KEY, JSON.stringify(dedup.slice(0, 12)));
    } catch (e) {}
  ```

  Because `<script is:inline>` does not interpolate Astro expressions, you need the literal values inlined. Replace the `%%…%%` placeholders by emitting a JSON object via a `<script type="application/json" id="rv-record">` block above the IIFE. Simpler: add this *immediately before* the existing `<script is:inline>` opening tag (around L1560):

  ```astro
  <script id="product-meta" type="application/json" set:html={JSON.stringify({
    handle: product.handle,
    title: product.title,
    image: images[0]?.url ?? "",
    price: price,
    currency: currencyCode
  })}></script>
  ```

  Then inside the IIFE, replace the placeholder version with:
  ```javascript
    try {
      var metaEl = document.getElementById("product-meta");
      if (metaEl) {
        var meta = JSON.parse(metaEl.textContent || "{}");
        var KEY = "ohc:recently-viewed";
        var existing = [];
        try { existing = JSON.parse(localStorage.getItem(KEY) || "[]"); } catch (e) {}
        var entry = {
          handle: meta.handle,
          title: meta.title,
          image: meta.image,
          price: meta.price,
          currency: meta.currency,
          viewedAt: Date.now()
        };
        var dedup = existing.filter(function (e) { return e.handle !== entry.handle; });
        dedup.unshift(entry);
        localStorage.setItem(KEY, JSON.stringify(dedup.slice(0, 12)));
      }
    } catch (e) {}
  ```

- [ ] **Step 4: Mount the component on product, collection, and home pages.**

  In `src/pages/products/[handle].astro` — frontmatter: add `import RecentlyViewed from "../../components/RecentlyViewed.astro";`. Then **inside** the `<Layout>` tag, immediately before the closing `</Layout>` on line **559**, add:
  ```astro
  <RecentlyViewed />
  ```

  In `src/pages/collections/[handle].astro` — frontmatter: `import RecentlyViewed from "../../components/RecentlyViewed.astro";`. Inside `<Layout>`, just before the closing `</Layout>` on line **208**, add:
  ```astro
  <RecentlyViewed />
  ```

  In `src/pages/index.astro` — frontmatter: `import RecentlyViewed from "../../components/RecentlyViewed.astro";`. Just before the closing `</Layout>` (search for `</Layout>` near the end of the markup section), add:
  ```astro
  <RecentlyViewed />
  ```

- [ ] **Step 5: Manual test the LocalStorage flow.**

  1. `npm run dev` → open `http://localhost:4321/`.
  2. Open DevTools → Application → Local Storage → clear `ohc:recently-viewed`.
  3. Click into a product (e.g. `/products/kfm2003a`).
  4. Click into another product.
  5. Go back to home `/`. Scroll to the bottom — "Recently Viewed" should show both products you visited.
  6. Open the most recent product page — its own card should be **excluded** from the strip (since you're already viewing it).
  7. Refresh — strip persists.

- [ ] **Step 6: Commit.**

  ```bash
  git add src/components/RecentlyViewed.astro src/pages/products/[handle].astro src/pages/collections/[handle].astro src/pages/index.astro src/i18n/ui.ts
  git commit -m "feat: Recently Viewed Products strip on home/collection/product pages per remarks §11

  - New <RecentlyViewed/> component with client-side LocalStorage
    tracking under key 'ohc:recently-viewed' (capped at 12)
  - Product page records itself on load; strip filters it out
  - Mounted at the bottom of home, collection, and product pages
    so it sits below 'Similar Products' on product pages — both
    sections coexist per the client's request"
  ```

---

## Task 12 — PDF #12 & #14: Detailed breadcrumbs on every page

**PDF text:**
- §12: `Home > Products > Orthopaedics > Back & Lumbar > Rigid Spine FocusBrace` — currently only `Home > Products > <product>` on many pages.
- §14: Apply this detailed breadcrumb to **all** pages, not just products.

**Files:**
- Create: `src/components/Breadcrumb.astro` (shared)
- Modify: `src/pages/products/[handle].astro:62-145` (extract existing logic, use shared component, also widen `.breadcrumb-current` max-width to fix the visual truncation)
- Modify: `src/pages/collections/[handle].astro` — add breadcrumbs at top
- Modify: 9 `src/pages/pages/*.astro` files — add breadcrumbs
- Modify: `src/pages/search.astro`, `src/pages/cart.astro` — add breadcrumbs

- [ ] **Step 1: Create the shared Breadcrumb component.** Create `src/components/Breadcrumb.astro`:

  ```astro
  ---
  export interface Crumb {
    label: string;
    href: string | null; // null = current page (no link)
  }
  interface Props {
    crumbs: Crumb[];
  }
  const { crumbs } = Astro.props as Props;
  ---

  <nav class="breadcrumb" aria-label="Breadcrumb">
    <ol>
      {crumbs.map((crumb, i) => (
        <li>
          {crumb.href ? (
            <a href={crumb.href}>{crumb.label}</a>
          ) : (
            <span class="breadcrumb-current">{crumb.label}</span>
          )}
          {i < crumbs.length - 1 && (
            <svg class="breadcrumb-sep" xmlns="http://www.w3.org/2000/svg" fill="none" viewBox="0 0 24 24" stroke-width="2.5" stroke="currentColor" width="12" height="12" aria-hidden="true">
              <path stroke-linecap="round" stroke-linejoin="round" d="m8.25 4.5 7.5 7.5-7.5 7.5" />
            </svg>
          )}
        </li>
      ))}
    </ol>
  </nav>

  <style>
    .breadcrumb {
      background: #f8fafc;
      padding: 0.6rem 1.5rem;
      margin: -1.5rem -1.5rem 1.5rem;
      border-bottom: 1px solid #e8edf3;
    }
    .breadcrumb ol {
      list-style: none;
      display: flex;
      align-items: center;
      gap: 0.25rem;
      margin: 0;
      padding: 0;
      flex-wrap: wrap;
      max-width: 1200px;
      margin-inline: auto;
    }
    .breadcrumb li {
      display: inline-flex;
      align-items: center;
      gap: 0.25rem;
      font-size: 0.78rem;
      color: #888;
    }
    .breadcrumb a {
      color: #1e3a5f;
      text-decoration: none;
      transition: color 0.15s;
    }
    .breadcrumb a:hover { color: #2563eb; }
    .breadcrumb-current {
      color: #555;
      font-weight: 600;
      max-width: 38ch;
      overflow: hidden;
      text-overflow: ellipsis;
      white-space: nowrap;
      display: inline-block;
    }
    .breadcrumb-sep { color: #bbb; flex-shrink: 0; }
  </style>
  ```

  *(Note the `max-width: 38ch` — replaces the cramped `200px` in `[handle].astro:596` so titles like "Rigid Spine FocusBrace" fit fully.)*

- [ ] **Step 2: Refactor product page to use the shared component.** Edit `src/pages/products/[handle].astro`:

  In frontmatter, add:
  ```typescript
  import Breadcrumb from "../../components/Breadcrumb.astro";
  ```

  Replace the existing `function buildBreadcrumbs()` (L63-88) with this updated version that injects the home-anchor target for "Products":

  ```typescript
  function buildBreadcrumbs() {
    const crumbs: { label: string; href: string | null }[] = [
      { label: t("nav.home"), href: lp("/") },
      { label: lang === "el" ? "Προϊόντα" : "Products", href: lp("/#cat-section") },
    ];

    if (extData.breadcrumbs) {
      extData.breadcrumbs.forEach((b) => crumbs.push({ label: b.label, href: b.href }));
    } else if (collectionHandle) {
      for (const cat of NAV_CATEGORIES) {
        const child = cat.children.find((c) => c.handle === collectionHandle);
        if (child) {
          crumbs.push({ label: getCategoryLabel(cat, lang), href: lp(`/collections/${cat.handle}`) });
          crumbs.push({ label: getSubLabel(child, lang), href: lp(`/collections/${child.handle}`) });
          break;
        }
        if (cat.handle === collectionHandle) {
          crumbs.push({ label: getCategoryLabel(cat, lang), href: lp(`/collections/${cat.handle}`) });
          break;
        }
      }
    }

    crumbs.push({ label: product.title, href: null });
    return crumbs;
  }
  ```

  Then replace the entire breadcrumb `<nav>…</nav>` block at L127-145 with:
  ```astro
  <Breadcrumb crumbs={breadcrumbs} />
  ```

  And delete the now-duplicated `.breadcrumb*` CSS rules in the page's `<style>` block (L562-602) — the styles now live in the shared component.

- [ ] **Step 3: Add an anchor target for `#cat-section` on the home page** so PDF §13 ("where does `Products` go?") lands somewhere meaningful.

  Edit `src/pages/index.astro:486` — change:
  ```astro
  <section class="cat-section">
  ```
  to:
  ```astro
  <section class="cat-section" id="cat-section">
  ```

- [ ] **Step 4: Add breadcrumbs to the collection page.** Edit `src/pages/collections/[handle].astro`. Add to frontmatter:

  ```typescript
  import Breadcrumb from "../../components/Breadcrumb.astro";
  import { findNavCategory } from "../../lib/menuConfig";

  // Build crumbs: Home > Products > [Parent if subcat] > Current
  function buildCollectionCrumbs() {
    const crumbs: { label: string; href: string | null }[] = [
      { label: t("nav.home"), href: lp("/") },
      { label: lang === "el" ? "Προϊόντα" : "Products", href: lp("/#cat-section") },
    ];
    if (isTopLevel) {
      crumbs.push({ label: pageTitle, href: null });
    } else {
      // find the parent NAV category whose children contain `handle`
      const parent = NAV_CATEGORIES.find(c => c.children.some(ch => ch.handle === handle));
      if (parent) {
        crumbs.push({ label: getCategoryLabel(parent, lang), href: lp(`/collections/${parent.handle}`) });
      }
      crumbs.push({ label: pageTitle, href: null });
    }
    return crumbs;
  }
  const collectionCrumbs = buildCollectionCrumbs();
  ```

  In the markup, **immediately after `<Layout ...>`** and **before** the existing `{isTopLevel ? (` ternary (around L74), insert:

  ```astro
  <Breadcrumb crumbs={collectionCrumbs} />
  ```

- [ ] **Step 5: Add breadcrumbs to the 9 static `pages/pages/*.astro` files.** For each of:
  - `about-us.astro`
  - `find-us.astro`
  - `working-hours.astro`
  - `shipping-delivery.astro`
  - `legal-notice.astro`
  - `refund-cancellation-policy.astro`
  - `terms-and-conditions.astro`
  - `terms-of-service.astro`
  - `cookies-policy.astro`

  Do the following pattern (illustrated for `about-us.astro` — adapt the page-title key per page):

  In the frontmatter, after the existing imports, add:
  ```astro
  import Breadcrumb from "../../components/Breadcrumb.astro";
  const crumbs = [
    { label: t("nav.home"), href: lp("/") },
    { label: t("nav.pages"), href: null }, // 'Pages' is a label, not a route — keep non-link
    { label: t("about.title"), href: null }, // change key per page: workingHours.title, shippingDelivery.title, findUs.title — for legal/refund/terms/cookies use the existing nav.* keys
  ];
  ```

  Then in the markup, immediately after `<Layout ...>` and before any other content, insert:
  ```astro
  <Breadcrumb crumbs={crumbs} />
  ```

  Page-by-page final-crumb mapping:
  - `about-us` → `t("about.title")`
  - `find-us` → `t("findUs.title")`
  - `working-hours` → `t("workingHours.title")`
  - `shipping-delivery` → `t("shippingDelivery.title")`
  - `legal-notice` → `t("nav.legalNotice")`
  - `refund-cancellation-policy` → `t("nav.refundPolicy")`
  - `cookies-policy` → `t("nav.cookiesPolicy")`
  - `terms-and-conditions` → `t("nav.termsConditions")`
  - `terms-of-service` → `t("nav.termsOfService")`

  *(Where any page does not already import `useTranslations`/`pickLang`/`localizeHref`, also add those imports.)*

- [ ] **Step 6: Add breadcrumbs to search & cart.**

  `src/pages/search.astro` — same pattern, final crumb: `t("search.page.title")`.
  `src/pages/cart.astro` — same pattern, final crumb: `t("cart.title")`.

  In both, the second crumb should be the page itself (not "Pages") — so they're 2-deep: `Home > Search` and `Home > Cart`.

- [ ] **Step 7: Verify each page.** `npm run dev`. Spot-check:
  - `/products/sp14029` → `Home > Products > Orthopaedics > Knee > <product title>` (or similar; depends on Shopify collection).
  - `/collections/back-lumbar` → `Home > Products > Orthopaedics > Back & Lumbar`.
  - `/collections/wheelchairs` → `Home > Products > Wheelchairs`.
  - `/pages/about-us` → `Home > Pages > About Us`.
  - `/cart` → `Home > Your Cart`.
  - `/search` → `Home > Search Results`.
  - Click the "Products" crumb → lands on home page, scrolled to the Shop-by-Category grid.

- [ ] **Step 8: Commit.**

  ```bash
  git add src/components/Breadcrumb.astro src/pages/products/[handle].astro src/pages/collections/[handle].astro src/pages/index.astro src/pages/pages/*.astro src/pages/search.astro src/pages/cart.astro
  git commit -m "feat: shared Breadcrumb component used site-wide per remarks §12, §13, §14

  - New <Breadcrumb/> component (src/components/Breadcrumb.astro)
  - Product page now uses full path: Home > Products > Category >
    Subcategory > Product, with .breadcrumb-current widened from
    200px to 38ch so titles no longer truncate
  - Collection, static pages, search, and cart all get breadcrumbs
  - 'Products' crumb links to /#cat-section (Shop-by-Category grid)"
  ```

---

## Task 13 — PDF #15: Product image lightbox / zoom

**PDF text:** *"Όταν επιλέγονται οι φωτογραφίες των προιόντων, αυτές δεν ανοίγουν μεγενθυμένες σε άλλο παράθυρο. Αυτό χρειάζεται πολύ στα Sensory Rooms όπου θα υπάρχουν και φωτογραφίες υψηλής ανάλυσης."*

**Files:**
- Modify: `src/pages/products/[handle].astro:147-190` (gallery + new `<dialog>` lightbox) and the IIFE script section (add open/close handlers)

- [ ] **Step 1: Add a `<dialog>`-based lightbox at the end of the markup.** Edit `src/pages/products/[handle].astro`. Just before the closing `</Layout>` on line **559**, insert:

  ```astro
  <dialog id="image-lightbox" class="image-lightbox" aria-label={lang === "el" ? "Μεγέθυνση εικόνας" : "Image zoom"}>
    <button type="button" class="lightbox-close" id="lightbox-close" aria-label={lang === "el" ? "Κλείσιμο" : "Close"}>×</button>
    <img id="lightbox-img" src="" alt="" />
    <div class="lightbox-nav">
      <button type="button" id="lightbox-prev" class="lightbox-arrow" aria-label={lang === "el" ? "Προηγούμενο" : "Previous"}>‹</button>
      <button type="button" id="lightbox-next" class="lightbox-arrow" aria-label={lang === "el" ? "Επόμενο" : "Next"}>›</button>
    </div>
  </dialog>
  ```

- [ ] **Step 2: Make the main image clickable.** In the same file, change L153-158:

  From:
  ```astro
  <img
    id="main-image"
    src={images[0]?.url}
    alt={images[0]?.altText || product.title}
    class="main-image"
  />
  ```

  To:
  ```astro
  <button
    type="button"
    class="main-image-trigger"
    id="main-image-trigger"
    aria-label={lang === "el" ? "Μεγέθυνση εικόνας" : "Zoom image"}
  >
    <img
      id="main-image"
      src={images[0]?.url}
      alt={images[0]?.altText || product.title}
      class="main-image"
    />
    <span class="zoom-hint" aria-hidden="true">
      <svg xmlns="http://www.w3.org/2000/svg" fill="none" viewBox="0 0 24 24" stroke-width="1.5" stroke="currentColor" width="22" height="22">
        <path stroke-linecap="round" stroke-linejoin="round" d="M21 21l-5.197-5.197m0 0A7.5 7.5 0 105.196 5.196a7.5 7.5 0 0010.607 10.607zM10.5 7.5v6m-3-3h6"/>
      </svg>
    </span>
  </button>
  ```

- [ ] **Step 3: Add lightbox styles.** In the `<style>` block of the same file, append:

  ```css
  .main-image-trigger {
    position: relative;
    display: block;
    padding: 0;
    border: 0;
    background: transparent;
    cursor: zoom-in;
    width: 100%;
  }
  .main-image-trigger:focus-visible { outline: 2px solid #2563eb; outline-offset: 2px; }
  .zoom-hint {
    position: absolute;
    bottom: 0.75rem;
    right: 0.75rem;
    background: rgba(15,23,42,.7);
    color: #fff;
    padding: 0.4rem;
    border-radius: 999px;
    display: inline-flex;
    align-items: center;
    justify-content: center;
    pointer-events: none;
  }

  .image-lightbox {
    border: none;
    background: rgba(15, 23, 42, 0.92);
    padding: 0;
    max-width: 100vw;
    max-height: 100vh;
    width: 100vw;
    height: 100vh;
    inset: 0;
    margin: 0;
  }
  .image-lightbox::backdrop { background: rgba(15,23,42,.92); }
  .image-lightbox img {
    display: block;
    margin: auto;
    max-width: 92vw;
    max-height: 88vh;
    object-fit: contain;
    cursor: zoom-out;
  }
  .lightbox-close {
    position: absolute;
    top: 1rem;
    right: 1rem;
    width: 44px;
    height: 44px;
    border: 0;
    border-radius: 50%;
    background: rgba(255,255,255,.15);
    color: #fff;
    font-size: 1.75rem;
    line-height: 1;
    cursor: pointer;
  }
  .lightbox-close:hover { background: rgba(255,255,255,.28); }
  .lightbox-nav {
    position: absolute;
    inset: 0;
    display: flex;
    justify-content: space-between;
    align-items: center;
    padding: 0 1rem;
    pointer-events: none;
  }
  .lightbox-arrow {
    pointer-events: auto;
    width: 48px;
    height: 48px;
    border: 0;
    border-radius: 50%;
    background: rgba(255,255,255,.15);
    color: #fff;
    font-size: 2rem;
    line-height: 1;
    cursor: pointer;
  }
  .lightbox-arrow:hover { background: rgba(255,255,255,.28); }
  ```

- [ ] **Step 4: Wire up lightbox JS.** In the IIFE inside the inline `<script>` at the bottom of the file (the same one that already handles thumbnails — search for `var mainImg = document.getElementById("main-image")` or the thumbnail click handler), append the following block before the final `})();`:

  ```javascript
    /* ── Image lightbox ── */
    (function initLightbox() {
      var trigger = document.getElementById("main-image-trigger");
      var lightbox = document.getElementById("image-lightbox");
      var lbImg = document.getElementById("lightbox-img");
      var lbClose = document.getElementById("lightbox-close");
      var lbPrev = document.getElementById("lightbox-prev");
      var lbNext = document.getElementById("lightbox-next");
      var mainImgEl = document.getElementById("main-image");
      if (!trigger || !lightbox || !lbImg || !mainImgEl) return;

      // Gather images from thumbnail buttons (data-src) or fall back to the main image
      var thumbBtns = Array.prototype.slice.call(document.querySelectorAll(".thumb-btn"));
      var srcs = thumbBtns.length
        ? thumbBtns.map(function (b) { return b.dataset.src; })
        : [mainImgEl.src];

      var currentIdx = 0;

      function open(idx) {
        currentIdx = Math.max(0, Math.min(srcs.length - 1, idx));
        lbImg.src = srcs[currentIdx];
        lbImg.alt = mainImgEl.alt || "";
        if (typeof lightbox.showModal === "function") {
          lightbox.showModal();
        } else {
          lightbox.setAttribute("open", "");
        }
      }
      function close() {
        if (typeof lightbox.close === "function") lightbox.close();
        else lightbox.removeAttribute("open");
      }

      trigger.addEventListener("click", function () {
        // start at whichever thumb is currently active
        var activeIdx = thumbBtns.findIndex(function (b) { return b.classList.contains("active"); });
        open(activeIdx >= 0 ? activeIdx : 0);
      });
      lbClose.addEventListener("click", close);
      lightbox.addEventListener("click", function (e) {
        if (e.target === lightbox || e.target === lbImg) close();
      });
      lbPrev && lbPrev.addEventListener("click", function (e) { e.stopPropagation(); open(currentIdx - 1); });
      lbNext && lbNext.addEventListener("click", function (e) { e.stopPropagation(); open(currentIdx + 1); });
      document.addEventListener("keydown", function (e) {
        if (!lightbox.open) return;
        if (e.key === "Escape") close();
        else if (e.key === "ArrowLeft" && lbPrev) lbPrev.click();
        else if (e.key === "ArrowRight" && lbNext) lbNext.click();
      });
    })();
  ```

- [ ] **Step 5: Verify.** `npm run dev` → open any product page → click the main image. Expected: fullscreen overlay with the image, close (×), and prev/next arrows. Keyboard: Esc closes; ←/→ navigates. The cursor shows `zoom-in` on the trigger and `zoom-out` on the open image. Confirm at least one product with multiple images cycles through all of them via the arrows.

- [ ] **Step 6: Commit.**

  ```bash
  git add src/pages/products/[handle].astro
  git commit -m "feat(product): image lightbox with prev/next + keyboard nav per remarks §15

  Click the main product image to open a fullscreen <dialog>-based
  lightbox. Arrow keys / on-screen arrows cycle through all thumbs.
  Esc or backdrop click closes."
  ```

---

## Task 14 — PDF #16 & #17: Clearer Add-to-Cart confirmation flow

**PDF text:**
- §16: Existing site's Add-to-Cart leads to a clearer/shorter checkout flow; new site only flashes "Added" then nothing.
- §17: Where should "Continue Shopping" go?

**Design decision:** Instead of hard-redirecting (which the legacy site does), we'll present a non-blocking confirmation modal next to the Add-to-Cart button with two prominent actions: **"View Cart"** (→ `/cart`) and **"Continue Shopping"** (closes the modal, user stays on the product page). This is shorter than a full redirect *and* makes the action explicit. The cart-empty "Continue Shopping" link remains pointed at home.

**Files:**
- Modify: `src/pages/products/[handle].astro` (markup at L283-289 region + IIFE handler at L1677-1705)
- Modify: `src/i18n/ui.ts` — add `cart.added`, `cart.continueShopping`, `cart.viewCart`, `cart.itemAdded`

- [ ] **Step 1: Translation keys.** In `src/i18n/ui.ts` (both blocks), confirm `cart.continueShopping` already exists. Add:

  ```typescript
  // en:
  "cart.itemAdded": "Added to your cart",
  "cart.viewCart": "View Cart",
  // el:
  "cart.itemAdded": "Προστέθηκε στο καλάθι σας",
  "cart.viewCart": "Προβολή Καλαθιού",
  ```

- [ ] **Step 2: Add the confirmation modal markup.** Edit `src/pages/products/[handle].astro` — just after the closing `</div>` of `.product-top` (around line **292**), insert:

  ```astro
  <dialog id="add-confirm" class="add-confirm" aria-label={t("cart.itemAdded")}>
    <div class="add-confirm__inner">
      <div class="add-confirm__icon" aria-hidden="true">
        <svg xmlns="http://www.w3.org/2000/svg" fill="none" viewBox="0 0 24 24" stroke-width="2.5" stroke="currentColor" width="32" height="32">
          <path stroke-linecap="round" stroke-linejoin="round" d="m4.5 12.75 6 6 9-13.5" />
        </svg>
      </div>
      <p class="add-confirm__title">{t("cart.itemAdded")}</p>
      <p class="add-confirm__product">{product.title}</p>
      <div class="add-confirm__actions">
        <button type="button" class="add-confirm__btn add-confirm__btn--ghost" id="add-confirm-continue">{t("cart.continueShopping")}</button>
        <a class="add-confirm__btn add-confirm__btn--primary" href={lp("/cart")}>{t("cart.viewCart")}</a>
      </div>
    </div>
  </dialog>
  ```

- [ ] **Step 3: Styles.** In the same file's `<style>` block, append:

  ```css
  .add-confirm {
    border: none;
    border-radius: 14px;
    padding: 0;
    width: min(420px, calc(100% - 2rem));
    box-shadow: 0 18px 60px rgba(15, 23, 42, 0.28);
  }
  .add-confirm::backdrop { background: rgba(15,23,42,.55); }
  .add-confirm__inner { padding: 1.5rem 1.5rem 1.25rem; text-align: center; }
  .add-confirm__icon {
    width: 56px; height: 56px;
    margin: 0 auto 0.75rem;
    border-radius: 50%;
    background: #ecfdf5;
    color: #047857;
    display: inline-flex;
    align-items: center;
    justify-content: center;
  }
  .add-confirm__title { font-weight: 700; font-size: 1.05rem; margin: 0 0 0.25rem; color: #1e3a5f; }
  .add-confirm__product { font-size: 0.9rem; color: #555; margin: 0 0 1.25rem; }
  .add-confirm__actions {
    display: grid;
    grid-template-columns: 1fr 1fr;
    gap: 0.5rem;
  }
  .add-confirm__btn {
    appearance: none;
    border: 1px solid transparent;
    border-radius: 8px;
    padding: 0.7rem 1rem;
    font-weight: 600;
    font-size: 0.92rem;
    cursor: pointer;
    text-decoration: none;
    text-align: center;
    display: inline-flex;
    align-items: center;
    justify-content: center;
  }
  .add-confirm__btn--ghost {
    background: #fff;
    border-color: #d1d5db;
    color: #1e3a5f;
  }
  .add-confirm__btn--ghost:hover { background: #f3f4f6; }
  .add-confirm__btn--primary {
    background: #2563eb;
    color: #fff;
  }
  .add-confirm__btn--primary:hover { background: #1d4ed8; }
  ```

- [ ] **Step 4: Replace the Add-to-Cart handler.** Edit the IIFE add-to-cart block at L1677-1705 (the block beginning `/* ── Add to cart ── */`):

  Replace it with:

  ```javascript
    /* ── Add to cart (with confirmation modal) ── */
    var confirmDlg = document.getElementById("add-confirm");
    var confirmContinue = document.getElementById("add-confirm-continue");

    if (addBtn) {
      addBtn.addEventListener("click", function () {
        if (addBtn.disabled || addBtn.classList.contains("loading")) return;
        var variantId = addBtn.dataset.variantId;
        if (!variantId) return;

        var currentQty = qty || 1;
        addBtn.classList.add("loading");
        var origText = addBtn.textContent;
        addBtn.textContent = "Adding…";

        Cart.addToCart(variantId, currentQty)
          .then(function () {
            addBtn.textContent = origText;
            addBtn.classList.remove("loading");
            if (confirmDlg) {
              if (typeof confirmDlg.showModal === "function") confirmDlg.showModal();
              else confirmDlg.setAttribute("open", "");
            }
          })
          .catch(function (err) {
            console.error("Add to cart failed:", err);
            addBtn.textContent = "Error – try again";
            addBtn.classList.remove("loading");
            setTimeout(function () {
              addBtn.textContent = origText;
            }, 2000);
          });
      });
    }

    if (confirmContinue && confirmDlg) {
      confirmContinue.addEventListener("click", function () {
        if (typeof confirmDlg.close === "function") confirmDlg.close();
        else confirmDlg.removeAttribute("open");
      });
      confirmDlg.addEventListener("click", function (e) {
        if (e.target === confirmDlg) {
          if (typeof confirmDlg.close === "function") confirmDlg.close();
          else confirmDlg.removeAttribute("open");
        }
      });
    }
  ```

- [ ] **Step 5: Verify.** `npm run dev` → open any in-stock product → click "Add to Cart". A modal should appear with a green check icon, the product title, and two buttons: **Continue Shopping** (closes the modal, stays on the product) and **View Cart** (navigates to `/cart`). Test both. Re-click Add to Cart twice in quick succession — modal opens once per click, cart badge in header increments. On `/cart`, both lines show as expected.

- [ ] **Step 6: Commit.**

  ```bash
  git add src/pages/products/[handle].astro src/i18n/ui.ts
  git commit -m "feat(cart): post-add confirmation modal per remarks §16, §17

  After a successful Add to Cart, opens a centered confirmation
  modal with two CTAs:
    - 'Continue Shopping' closes the modal and stays on the
      current product page
    - 'View Cart' navigates to /cart
  Replaces the previous 1.2s 'Added!' flash with an explicit choice."
  ```

---

## Final Verification & Hand-off

- [ ] **Step F1: Build the production bundle.**

  Run:
  ```bash
  npm run build
  ```
  Expected: no errors. If TypeScript or Astro errors appear, fix the offending file before declaring done.

- [ ] **Step F2: Preview the production build.**

  ```bash
  npm run preview
  ```
  Walk through each remark's verification checklist again against the production output (caching can sometimes mask issues that only appear in dev).

- [ ] **Step F3: Update `docs/category-content-checklist.md` if it references the 4-icon trust bar or old footer copy** — keep documentation consistent.

- [ ] **Step F4: Stage outstanding client-side TODOs.**

  Re-grep:
  ```bash
  grep -rn "TODO(client-copy\|TODO(client-asset" src/ public/
  ```
  Surface this list to the user so they can chase the client deliverable (info-box copy + logo PNG).

- [ ] **Step F5: Final summary to the user.** Report:
  - Each PDF remark (§1-§17) → status (done / verified-no-change / awaiting-client).
  - Two open client decisions taken (breadcrumb 'Products' target and Continue-Shopping target) so they can override if desired.

---

## Self-Review (writing-plans skill checklist)

**Spec coverage table:**

| PDF § | Status in this plan |
| --- | --- |
| §1 (info-box separation) | Task 1 |
| §2 (logo res) | Task 2 (placeholder until client sends PNG) |
| §3 (6 trust badges) | Task 3 |
| §4 (footer = working hours) | Task 4 |
| §5 (sensory / stairlift quote) | Task 5 |
| §6 (orth subcats in menu) | Task 6 (verification — already implemented) |
| §7 (rename Featured products) | Task 7 |
| §8 (other cats stop at Browse) | Task 8 |
| §9 (in/out-of-stock on cards) | Task 9 |
| §10 (More from → Similar) | Task 10 |
| §11 (Recently Viewed) | Task 11 |
| §12 (full breadcrumb on product) | Task 12 |
| §13 ("Products" link target) | Task 12 step 3 — points to `/#cat-section` |
| §14 (breadcrumbs site-wide) | Task 12 steps 4-6 |
| §15 (image lightbox) | Task 13 |
| §16 (clearer Add-to-Cart) | Task 14 |
| §17 ("Continue Shopping" target) | Task 14 step 2 (modal CTA) + cart.astro:19 unchanged |

**Placeholder scan:** All `TODO()` markers in the plan are deliberate `TODO(client-copy …)` / `TODO(client-asset …)` blockers — no implementation placeholders.

**Type consistency:** All function names (`isQuotationOnly`, `buildBreadcrumbs`, `buildCollectionCrumbs`) and component names (`Breadcrumb`, `TrustBadgesStrip`, `RecentlyViewed`) are referenced consistently across tasks. Translation keys (`product.contactForQuote`, `card.inStock`, `cart.itemAdded`, etc.) are introduced exactly once and reused.
